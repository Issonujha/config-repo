server:
  port: 8083

spring:
  application:
    name: expense-service
  datasource:
    url: jdbc:mysql://localhost:3306/expensedb
    username: root
    password: Sonu123@

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
